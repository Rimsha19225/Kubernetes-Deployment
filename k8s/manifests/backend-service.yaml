apiVersion: v1
kind: Service
metadata:
  name: backend-service
  labels:
    app: todo-backend
    ai-monitored: "true"
    component: backend
spec:
  type: ClusterIP
  selector:
    app: todo-backend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000